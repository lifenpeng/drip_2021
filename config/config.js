const config = {
    port:'9999',
    mysql:{
        connectionLimit: 20,
        host     : 'localhost',
        user     : 'root',
        password : 'root',
        database : 'drip'
    }
}

module.exports = config;